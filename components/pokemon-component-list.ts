import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        marginHorizontal: 20,
        borderWidth: 0.2,
        borderRadius: 6,
        padding: 10
    },

    containerName: {
        fontSize: 15,
        fontWeight: 500
    },

    containerUrl: {
        fontSize: 15
    }
});